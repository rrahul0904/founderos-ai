"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const[key,setKey]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(){setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({key})});if(r.ok)router.push("/");else setError("Invalid access key");setBusy(false)}return <div style={{maxWidth:520,margin:"0 auto"}}><div className="idea-form"><input aria-label="Access key" type="password" value={key} onChange={(e: { target: { value: string } })=>setKey(e.target.value)} placeholder="Access key" style={{background:"transparent",border:0,color:"inherit",padding:16,outline:0}}/><button className="primary" disabled={busy} onClick={submit}>{busy?"Checking…":"Continue"}</button></div>{error?<div className="error small">{error}</div>:null}</div>}
